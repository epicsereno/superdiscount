/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Add your real image host here once product photos exist.
    // remotePatterns: [{ protocol: 'https', hostname: 'cdn.yourdomain.com' }],
  },
};

export default nextConfig;
